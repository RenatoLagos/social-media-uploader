---
description: Preview de descripciones generadas por IA sin subir el video
arguments:
  - name: video_path
    description: Ruta completa al archivo de video MP4
    required: true
---

Ejecuta el siguiente comando para generar y mostrar las descripciones sin subir:

```bash
cd C:\Users\Admin\Projects\upload_to_socialmedia && .\venv\Scripts\activate && python upload.py "$ARGUMENTS.video_path" --preview
```

Muéstrame las descripciones generadas para YouTube, Instagram y TikTok.
